---
description: Get instructions to merge current branch to main via Cursor
---

The current branch has changes ready to deploy.

**Tell Cursor:**

> Merge branch `$ARGUMENTS` into main and push

If no branch was specified, use the current branch from git.
